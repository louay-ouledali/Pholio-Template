{
  "name": "pholio-api",
  "version": "1.0.0",
  "description": "Serverless API for Pholio",
  "scripts": {
    "start": "func start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "groq-sdk": "^0.3.3",
    "@google/generative-ai": "^0.11.1",
    "firebase": "^9.22.0",
    "dotenv": "^16.0.3"
  }
}